run = "npx pnpm --filter server dev"

[deployment]
run = ["npx", "pnpm", "--filter", "server", "start"]
build = ["sh", "-c", "npx pnpm install --frozen-lockfile && npx pnpm turbo run build --filter=server"]
