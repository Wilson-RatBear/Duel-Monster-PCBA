run = "pnpm --filter server dev"

[deployment]
run = ["npx", "--no-install", "tsx", "apps/server/src/index.ts"]
build = ["sh", "-c", "npx pnpm@7 install --no-frozen-lockfile && npx pnpm@7 turbo run build --filter=server"]
